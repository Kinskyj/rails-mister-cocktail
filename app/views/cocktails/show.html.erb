<%= @cocktail.name %>

<ul>
<% @doses.each do |dose| %>
  <li>
    <%= dose.ingredient.name %>
    <%= dose.description %>
    <%= link_to "Delete dose", dose_path(dose), :method => 'delete'%>
  </li>
  <% end %>
</ul>

<h2>Add a dose</h2>

<%= render 'form_dose' %>

<%= link_to "Home", root_path %>
